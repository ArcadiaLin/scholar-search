[project]
name = "scholar-search"
version = "0.1.0"
description = "Research and engineering workspace for complex academic paper search"
requires-python = ">=3.11"
dependencies = [
    "metasci-universe",
]

[dependency-groups]
dev = [
    "huggingface-hub>=1.0,<2",
    "pytest>=8.4,<10",
    "pyyaml>=6,<7",
    "ruff>=0.12,<1",
]

[tool.uv]
package = false

[tool.uv.workspace]
members = [
    "packages/metasci-universe",
]

[tool.uv.sources]
metasci-universe = { workspace = true, editable = true }

[tool.pytest.ini_options]
addopts = "-ra"
pythonpath = ["."]
testpaths = ["tests"]

[tool.ruff]
target-version = "py311"
line-length = 120
extend-exclude = [
    ".widi-scholar",
    "artifacts",
    "data",
    "papers",
    "packages/metasci-universe",
    "packages/widi",
    "references",
    "runs",
]

[tool.ruff.lint]
select = ["B", "E4", "E7", "E9", "F", "I", "RUF", "UP"]
