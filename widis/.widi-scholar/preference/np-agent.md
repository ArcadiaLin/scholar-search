<!-- np-version: 0 -->

# 检索策略先验

这份文件是检索**策略**的载体。它与角色定义分开存放，是因为策略要能被逐条改写、
逐条关闭，而角色定义不需要——布局与版本约定见本目录的 `README.md`。

读到这份文件的 agent 应当把下面的条目当作先验建议，而不是硬约束：
硬约束在角色定义里（工具调用协议与安全边界），违反它们是错误；
这里的条目是"通常这样做效果更好"，遇到与之冲突的具体证据时，
以证据为准，并在报告里说明偏离了哪一条以及为什么。

条目格式：

```
- [id] 条目正文。
```

`id` 稳定，不因增删其他条目而改变，这样一条轨迹可以引用"当时生效的是哪几条"。
带 `kind: example` 的条目是示例式的，会额外标注 `origin`。

## 条目

<!-- 版本 0：载体已建立，尚无条目。条目内容见路线图 S5。 -->

（本版没有任何条目。当前生效的策略先验为空集——检索策略完全由 agent 自行决定，
不受本文件影响。这是消融实验的"全关"基线。）
